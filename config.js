// Point this to your deployed Cloudflare Worker URL (no trailing slash).
// Local dev: wrangler dev serves the worker at http://127.0.0.1:8787
window.APP_CONFIG = {
  API_BASE_URL: 'https://naari-api.namanamberkar.workers.dev'
};
